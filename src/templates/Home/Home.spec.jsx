import {render, screen} from '@testing-library/react';
import Index from './index';

describe('<Home />', () => {
    it('dummy test', () => {
        expect(1).toBe(1);
    });
    it('dummy test2', () => {
        expect(2).toBe(2);
    });
});
